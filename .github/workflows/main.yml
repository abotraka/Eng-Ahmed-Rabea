name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install dependencies 🏗️
        run: npm install

      - name: Build project 🛠️
        run: npm run build

      # هذا الأكشن يقوم برفع مجلد build/dist إلى فرع gh-pages
      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist  # تأكد أن هذا هو اسم مجلد الـ build
